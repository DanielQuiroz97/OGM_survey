#### Load Packages ####
library(readxl) # Read Excel Files
library(tidyverse) # Easily Install and Load the 'Tidyverse'
library(stringr) # Simple, Consistent Wrappers for Common String Operations

#### Load Data ####
raw_data <- map(3:11,
                .f = function(x) {read_xlsx('data/Encuestas.xlsx',
                                            sheet = x) %>% 
                    mutate(Sheet = x)}  ) %>% bind_rows()

#### Section 1 ####
# Sample space distribution


# Summarinsing data
sample_space <- clean_data1 %>% group_by(P2, P4a) %>% 
  summarise(N = n()) %>% arrange(P4a)
ggplot(clean_data1, aes(P4a)) + geom
